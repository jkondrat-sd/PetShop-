# Set to "true" to enable MirageJS mock server
# In production (Vercel), the mock server will auto-enable
REACT_APP_USE_MOCK=false

# API Base URL (only used if not mocking)
REACT_APP_API_URL=http://localhost:8089/api
