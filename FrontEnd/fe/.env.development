# Development environment
# Set to "true" to test mock server locally (no backend needed)
# Set to "false" when developing with real backend (Docker)
REACT_APP_USE_MOCK=false

# API Base URL for local development with real backend
REACT_APP_API_URL=http://localhost:8089/api
